package play

import "core:encoding/json"
import "core:fmt"


main :: proc() {

	// age_by_name: map[string]map[string]string
	// age_by_name = make(map[string]map[string]string)

	// section_name := "global"

	// if section_name not_in age_by_name {
	// 	age_by_name[section_name] = make(map[string]string)
	// }

	// age_by_name[section_name]["host"] = "localhost"

}
